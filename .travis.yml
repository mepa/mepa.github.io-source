branches:
  only:
  - master

language: python

python:
- 2.7

install:
- pip install -r requirements.txt --use-mirrors

script:
- make html

notifications
  email: false

after_success: bash deploy.sh